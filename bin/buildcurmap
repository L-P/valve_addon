#!/usr/bin/env sh
set -eux

cd "$(dirname "$0")/.."

latest="$(ls mapssrc/*.map --sort=time --time=ctime | head -n1)"

mapname="${latest#mapssrc/}"
mapname="${mapname%.*}"

bin/buildmap "$latest" "$@"
bin/run +map "$mapname"
